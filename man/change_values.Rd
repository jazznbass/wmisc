% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/change_values.R
\name{change_values}
\alias{change_values}
\title{Recode Values in a Vector}
\usage{
change_values(x, ..., .default = NULL)
}
\arguments{
\item{x}{A vector whose values will be recoded.}

\item{...}{One or more replacement rules in the form \code{old ~ new}. Each rule specifies a value in \code{x} to be replaced.}

\item{.default}{Optional. A default value assigned to all elements not explicitly matched in the replacement rules. If \code{NULL} (default), unmatched values remain unchanged.}
}
\value{
A vector with values replaced according to the specified rules. If \code{x} is numeric and at least one replacement is a character string, the result will be coerced to character.
}
\description{
Replaces specific values in a vector with new values, using formula-style syntax.
}
\examples{
# Basic recoding
change_values(c(1, 2, 3, NA), 2 ~ "two", 3 ~ "three")

# Assign a default value to unmatched entries
change_values(c(1, 2, 3), 2 ~ "two", .default = "default")

# Unmatched values remain unchanged (no warning or error)
change_values(c(1, 2, 3), 4 ~ "four")

# Recode missing values (NA)
change_values(c(NA, 1, NA, 2), NA ~ "This is a missing value")

}
\seealso{
\code{\link[dplyr:recode]{dplyr::recode()}}, \code{\link[=ifelse]{ifelse()}}, \code{\link[dplyr:case_when]{dplyr::case_when()}}
}
