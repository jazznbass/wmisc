% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/values-to-colors.R
\name{values_to_colors_continuous}
\alias{values_to_colors_continuous}
\title{Map continuous values to diverging colors (dark red -> white -> dark green)}
\usage{
values_to_colors_continuous(
  x,
  labels = NULL,
  min = -3,
  max = 3,
  neutral_min = -0.5,
  neutral_max = 0.5,
  low = "#7f0000",
  mid = "#ffffff",
  high = "#006400",
  n = 201
)
}
\arguments{
\item{x}{Numeric vector. Can be named or unnamed, but must align
with \code{labels} by name (preferred) or by position (same length).}

\item{labels}{Character vector of node labels (names to use in the result).}

\item{min}{Minimum value that maps to the darkest red (values below are
clipped).}

\item{max}{Maximum value that maps to the darkest green (values above are
clipped).}

\item{neutral_min}{Minimum value of the white "dead zone" (values above are
colored white).}

\item{neutral_max}{Maximum value of the white "dead zone" (values below are
colored white).}

\item{low}{Color for low end (default dark red).}

\item{mid}{Color for midpoint (default white).}

\item{high}{Color for high end (default dark green).}

\item{n}{Number of discrete colors used internally for the gradient.}
}
\value{
Named character vector of hex colors with names equal to \code{labels}.
}
\description{
Map continuous values to diverging colors (dark red -> white -> dark green)
}
\examples{
z_values <- c(-2.5, -1, 0, 1, 2.5)
names(z_values) <- paste0("Node", 1:5)
colors <- values_to_colors_continuous(z_values)
print(colors)
t_values <- z_values * 10 + 50
names(t_values) <- paste0("Node", 1:5)
colors <- values_to_colors_continuous(
  t_values, min = 20, max = 80, neutral_min = 45, neutral_max = 55)
print(colors)
}
